- multibranch-job:
    name: Api_tests
    description: "API tests with Maven and Allure (all branches)"
    branch-sources:
      - github:
          id: "github-api-tests"   # any unique ID
          repo-owner: "RazMKhitaryan"
          repository: "otus_api_helpers"
          credentials-id: ""       # add if private repo
          includes: "*"            # include all branches
          excludes: ""            # exclude none
    factory:
      workflow:
        script-path: "pipelines/jenkinsfile_api.groovy"
